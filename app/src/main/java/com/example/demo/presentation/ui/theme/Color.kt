package com.example.demo.presentation.ui.theme
import androidx.compose.ui.graphics.Color

/**
 * A soft off-white color used as the primary background in Light Mode.
 * Hex: #F7F7F7
 */
val LightBackground = Color(0xFFF7F7F7)

/**
 * A pure black color used as the primary background in Dark Mode.
 * Hex: #000000
 */
val DarkBackground = Color(0xFF000000)